# ============================================================
# AML Detection System – Python Dependencies
# Python 3.10+  |  pip install -r requirements.txt
# ============================================================

# ── Core data / ML ───────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
pyarrow>=14.0.0               # parquet I/O

# ── Deep learning ────────────────────────────────────────────
torch>=2.2.0
# torch-geometric>=2.5.0      # optional – enables GATv2Conv

# ── Synthetic data ───────────────────────────────────────────
Faker>=23.0.0

# ── Graph analytics ──────────────────────────────────────────
networkx>=3.2.0

# ── Fast analytical queries ──────────────────────────────────
duckdb>=0.10.0

# ── Multi-agent orchestration (LangGraph) ────────────────────
langgraph>=0.1.0
langchain>=0.2.0
langchain-core>=0.2.0

# ── Local LLM (Ollama HTTP client) ───────────────────────────
# Install Ollama: https://ollama.com/download
# Then: ollama pull llama3
requests>=2.31.0

# ── OpenAI-compatible LLM (optional cloud fallback) ──────────
openai>=1.20.0

# ── Dashboard ────────────────────────────────────────────────
streamlit>=1.33.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0

# ── API ──────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0

# ── Explainability (SHAP) ────────────────────────────────────
shap>=0.45.0

# ── Causal inference (optional) ──────────────────────────────
# pip install econml            # CausalForestDML
# pip install causalimpact      # Google CausalImpact

# ── Workflow scheduling (optional) ───────────────────────────
# pip install apache-airflow    # DAG scheduling

# ── Testing ──────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Development ──────────────────────────────────────────────
jupytext>=1.16.0
ipykernel>=6.29.0
black>=24.0.0
ruff>=0.4.0
mypy>=1.9.0
pre-commit>=3.7.0
